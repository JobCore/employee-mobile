import {StyleSheet} from 'react-native';
import {
    WHITE_MAIN,
} from '../constants/colorPalette';

export default StyleSheet.create({
    title: {
        color: WHITE_MAIN,
        fontWeight: '500',
        fontSize: 18
    }
});
