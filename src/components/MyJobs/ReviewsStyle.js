import { StyleSheet } from 'react-native';
import {
  BLUE_DARK,
} from '../../shared/colorPalette';

export default StyleSheet.create({
  list: {
    marginTop: 0,
  },
  textRowNumber: {
    fontSize: 64,
    textAlign: 'center',
    color: BLUE_DARK,
    paddingRight: 30,
    paddingLeft: 30,
    fontWeight: 'normal',
  }
});
