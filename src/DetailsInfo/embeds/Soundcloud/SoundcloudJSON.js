/**
 * This JSON has more properties however we wont be using them all
 * @typedef {object} SoundcloudJSON
 * @prop {string} title
 * @prop {string} original_format
 * @prop {string|null} artwork_url
 * @prop {string} stream_url
 * @prop {number} duration
 */

export default {}
