import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  soundcloudView: {
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center',
  },
  tweetView: {
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center',
  },
})
