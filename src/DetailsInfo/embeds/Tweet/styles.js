import { StyleSheet } from 'react-native'

export const DEFAULT_TWEET_HEIGHT = 200

export default StyleSheet.create({
  spinner: {
    height: 32, // scrollview wont shrink on height change, it will only grow,
  },
})
