import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  icon: {
    alignSelf: 'center',
  },

  imageBackground: {
    alignContent: 'center',
    flex: 1,
    height: 360, // as it comes from the api
    justifyContent: 'center',
    resizeMode: 'contain',
    width: 480, // as it comes from the api
  },

  touchableOpacity: {

  },
})
